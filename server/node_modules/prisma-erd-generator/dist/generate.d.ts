import { GeneratorOptions } from '@prisma/generator-helper';
import { DMLModel, DMLField } from 'types/dml';
export declare function parseDatamodel(engine: string, model: string, tmpDir: string): Promise<string>;
export declare const mapPrismaToDb: (dmlModels: DMLModel[], dataModel: string) => {
    fields: DMLField[];
    name: string;
    isEmbedded: boolean;
    dbName: string | null;
    idFields: any[];
    uniqueFields: any[];
    uniqueIndexes: any[];
    isGenerated: boolean;
    primaryKey: {
        name: string | null;
        fields: string[];
    } | null;
}[];
declare const _default: (options: GeneratorOptions) => Promise<void>;
export default _default;
